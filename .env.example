# Application Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Paths (will be created if they don't exist)
CONFIG_DIR=/app/config
DOWNLOADS_DIR=/app/downloads

# Completed libraries on the Docker host. Point these at NAS mounts in production.
AUDIOBOOKS_PATH=./completed/audiobooks
EBOOKS_PATH=./completed/ebooks

# Session Secret (change this in production!)
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# Optional: Pre-configure service credentials (can be set via web UI instead)
# STORYTEL_USERNAME=your_username
# STORYTEL_PASSWORD=your_password
